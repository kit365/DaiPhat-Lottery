name: DaiPhat Frontend Deploy

on:
  push:
    branches:
      - main
    paths:
      - "daiphat-fe/**"
      - ".github/workflows/frontend-deploy.yml"
      - ".github/workflows/component-deploy.yml"
  workflow_dispatch:
    inputs:
      source_ref:
        description: Git ref to build and deploy
        required: true
        default: main
        type: choice
        options:
          - main
          - feature/dp-5-infs

jobs:
  deploy-frontend:
    uses: ./.github/workflows/component-deploy.yml
    with:
      component: frontend
      source_ref: ${{ inputs.source_ref || github.ref_name }}
    secrets: inherit
