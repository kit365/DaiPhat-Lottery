import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const Color primary = Color(0xFFD31010);
  static const Color primaryDark = Color(0xFFA60F14);
  static const Color goldDark = Color(0xFFF9A826);

  // Backgrounds & Surfaces
  static const Color pageBg = Color(0xFFFDF9F9);
  static const Color surface = Colors.white;
  static const Color rowOdd = Color(0xFFF8FAFC);
  static const Color rowEven = Colors.white;

  // Borders
  static const Color cardBorder = Color(0xFFE5E7EB);

  // Typography
  static const Color textMain = Color(0xFF111111);
  static const Color textMuted = Color(0xFF64748B);

  // --- Login Specific Variants ---
  // If we standardize later, we can merge these
  static const Color loginPrimary = Color(0xFFD32F2F);
  static const Color loginTitle = Color(0xFFA31010);
  static const Color loginGold = Color(0xFFFFB300);
  static const Color loginLabel = Color(0xFF333333);
  static const Color loginPlaceholder = Color(0xFF999999);
  static const Color loginBorder = Color(0xFFE0E0E0);

  // --- Restored properties from original AppColors ---
  static const Color accent = Color(0xFFFFD700);
  static const Color accentLight = Color(0xFFFFF9E6);
  static const Color background = Color(0xFFF5F6FA);
  static const Color ink = Color(0xFF17191F);
  static const Color textSecondary = Color(0xFF505050);
  static const Color navy = Color(0xFF102937);
  static const Color error = Color(0xFFEE1314);
  static const Color success = Color(0xFF4CAF50);
}

