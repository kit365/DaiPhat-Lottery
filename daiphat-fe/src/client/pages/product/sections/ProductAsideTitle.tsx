export const ProductAsideTitle = ({ title }: { title: string }) => {
    return (
        <h2 className="px-[30px] py-[15px] mb-[10px] text-client-secondary bg-[#e67e201a] rounded-[80px] text-[16px] font-secondary">{title}</h2>
    )
}