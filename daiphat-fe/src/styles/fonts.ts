/**
 * Brand font CSS variable class.
 * Fonts are loaded via Google Fonts in the root layout so builds still work
 * when Windows Smart App Control blocks Next.js native SWC and the project
 * falls back to Babel (next/font requires SWC).
 */
export const fontVariables = 'font-brand';
