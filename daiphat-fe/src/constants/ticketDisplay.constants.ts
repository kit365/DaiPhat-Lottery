/** Nhãn thống nhất cho dãy số vé (có thể kèm SN phụ). */
export const TICKET_NUMBERS_LABEL = 'Vé số';

/** Tiền tố serial in phụ dưới dãy số — giống bảng đơn hàng admin. */
export const TICKET_SERIAL_PREFIX = 'SN';

/** Tiền trúng trên từng vé. */
export const TICKET_WIN_AMOUNT_LABEL = 'Giá trị trúng';

/** Phiếu nhập lô vé — thống nhất, không dùng "phiếu tạo". */
export const IMPORT_BATCH_SLIP_LABEL = 'Phiếu nhập';
