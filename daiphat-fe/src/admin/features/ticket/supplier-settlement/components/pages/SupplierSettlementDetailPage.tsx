import { Card, Typography } from '@mui/material';

export const SupplierSettlementDetailPage = () => {
    return (
        <Card sx={{ p: 3 }}>
            <Typography variant="h6" fontWeight={700} mb={2}>
                Chi tiết đối soát nhà cung cấp
            </Typography>
        </Card>
    );
};
